[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tasktree-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.14.4",
    "pydantic>=2.0.0",
    "ruff>=0.14.14",
]

[tool.hatch.build]
packages = ["src/tasktree_mcp"]
